# Lore V1 RC Status - 2026-06-14

This document is the current release-candidate status note for Lore V1. It is
intended to be the canonical RC planning snapshot for the current branch:

- Branch: `b-line/audit-followups-2026-06-04`
- Current pushed HEAD: `a5b8deb`
- Scope: V1 RC stabilization, release verification, and handoff

Older status and handoff documents may still describe pre-RC gaps that have
since been closed. This document records the remaining RC work without editing
those older files, because the current checkout contains unrelated line-ending
noise in several historical documents.

## Current RC Assessment

Lore is past the feature-discovery part of V1 and is now in release-candidate
stabilization. The V1 loop is covered by deterministic gates:

- governed markdown intake;
- proposal/review/apply governance;
- persona candidate acceptance;
- operator queue and inbox aggregation;
- TUI review surfaces for drafts, findings, pending shell actions, and usage;
- external MCP contract freeze;
- external transcript JSONL import as process-sink-only;
- post-scan reconciliation through persisted findings;
- usage reporting by purpose and model.

The V1 beta acceptance gate and the default release gate passed at
`a5b8deb`, with `-SkipDiffCheck` because the checkout has pre-existing
line-ending-only modifications outside the intended changes.

## Completed RC Slices

- V1 beta acceptance gate with deterministic, no-real-model coverage.
- Operator queue and `lore inbox --json` command-path coverage.
- TUI findings list quick actions for resolve and ignore.
- Governance hardening for conflict-state persist failures.
- MCP live contract and onboarding doc freeze.
- TUI pending shell action projection and approve/reject flow.
- TUI usage visibility in the status panel.
- Post-scan reconciliation pinned to open findings surfaced through the queue.
- External transcript import boundary pinned to process-sink-only.

## Remaining V1 RC Phases

V1 RC should be split into four remaining phases:

### Phase 11: RC Docs and Status Cleanup

Estimated effort: 0.5 day.

Goal: create a single current status source for the RC and stop older handoff
documents from driving planning decisions.

Exit criteria:

- current RC status document exists and names the authoritative scope;
- stale historical docs are treated as archive context, not active schedule;
- known worktree caveats are documented;
- no line-ending-only file churn is added to the RC commit.

### Phase 12: RC Gate and Flake Cleanup

Estimated effort: 0.5 to 1 day.

Goal: make release verification boring enough that the RC can be cut without
explaining intermittent failures every time.

Exit criteria:

- investigate `TestCreatePersonaDraftFromCandidateConcurrentCallsOnlyOneSucceeds`;
- fix, isolate, or explicitly quarantine that flaky path with a clear rationale;
- confirm V1 beta gate still passes;
- confirm default release gate still passes;
- record the exact verification commands in the handoff.

### Phase 13: Manual Smoke and Operator Workflow

Estimated effort: 0.5 day.

Goal: run the product as an operator would, not only through unit tests.

Exit criteria:

- bootstrap or open a representative local workdir;
- run the inbox/operator queue flow;
- exercise TUI review surfaces for pending actions, findings, and usage;
- import external JSONL and confirm it remains in process-sink artifacts;
- run a post-scan finding flow and resolve or ignore it;
- capture any operator-facing rough edges as RC notes or post-V1 follow-ups.

### Phase 14: RC Cut and Handoff

Estimated effort: 0.5 day.

Goal: freeze the candidate and make the release state easy to resume.

Exit criteria:

- branch is pushed and tracking ref is current;
- RC release notes summarize shipped scope, verification, and deferred items;
- tag or PR handoff is prepared, depending on the chosen release process;
- remaining post-V1 work is explicit and not blocking RC.

## Deferred Beyond V1 RC

These are product-expansion items and should not be allowed to move the RC
finish line unless the release scope is deliberately reopened:

- automatic persona extraction from imported external transcripts;
- long-running app-server attach loop;
- media or binary extraction beyond the current markdown/reference path;
- richer post-scan conflict artifacts or automatic draft creation;
- broader TUI polish that is not needed for the governed review loop.

## Known Caveats

- The checkout contains many pre-existing tracked modifications that appear to
  be line-ending or whitespace noise. RC commits should stage only intentional
  files.
- A full `go test ./internal/app -count=1` has intermittently failed on
  `TestCreatePersonaDraftFromCandidateConcurrentCallsOnlyOneSucceeds`, while
  isolated reruns passed. This is the main Phase 12 cleanup item.
- Plain SSH push to `github.com:22` times out in this environment. Use
  `ssh://git@ssh.github.com:443/Faust7777777/Lore.git` when pushing.

## Decision

V1 RC needs four remaining phases: docs/status cleanup, gate/flake cleanup,
manual smoke, and RC cut/handoff. The practical remaining estimate is two to
three focused working days.

The larger backlog, including external-transcript persona extraction,
long-running app-server attach, and media/binary extraction, is closer to one
to two weeks and should be treated as post-RC unless the product scope changes.
